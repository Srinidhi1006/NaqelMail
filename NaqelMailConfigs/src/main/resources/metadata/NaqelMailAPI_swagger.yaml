openapi: 3.0.1
info:
  title: NaqelMailAPI
  description: API Definition of NaqelMailAPI
  version: 1.0.0
servers:
- url: http://localhost:8290/naqel_mail
paths:
  /:
    get:
      responses:
        default:
          description: Default response
  /newNaqel:
    post:
      requestBody:
        description: Sample Payload
        content:
          application/json:
            schema:
              type: object
              properties:
                payload:
                  type: object
        required: false
      responses:
        default:
          description: Default response
